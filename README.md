# Live-Map-Test
A testing for the soon-to-be LoRa + GPS tracker which will use a live map to track.

Initialise virtual environment
    
    python -m venv .venv
    .\.venv\Scripts\activate

Install requirements
    
    pip install -r requirements.txt

Run the app
    
    flask run

Find the app

    http://localhost:5000/
